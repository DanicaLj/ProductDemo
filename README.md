# ProductDemo
- the program runs on localhost and starting route is /index.php and on it is product page with comment form and listed comments and on route /admin.php is table where admin can approve or disapprove comments which are shown on porduct page
- to access admin page you need to insert usermane: admin and password: admin, also when you login as a admin, you can logout. There is table in detabese called users which have column role, if role is set to 1 then user can access admin page
- test.sql is sql script for generating all tables and data, and the name of database is also test
- I used XAMPP to run the program, starting Apache and MySql
